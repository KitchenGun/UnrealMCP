[project]
name = "unreal-mcp"
version = "0.1.0"
description = "Unreal Engine 5 MCP server -- Control UE5 editor with natural language"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "anyio>=4.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/unreal_mcp"]

[project.scripts]
unreal-mcp = "unreal_mcp.main